training_partner:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'athlete') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'athlete') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'athlete') }}"
      schema: raw
      threads: 1
