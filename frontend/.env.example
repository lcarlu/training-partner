# Base URL for the FastAPI backend. Empty string relies on the Vite dev proxy for /api.
VITE_API_BASE_URL=

# Set to "true" to run the UI against in-memory mock data instead of the real backend
# (useful while the backend isn't running yet).
VITE_USE_MOCKS=false
